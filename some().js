// some() method
// similar to every() method but returns true if even single element passes the condition and returns false if all the elements fail the condition

let arr = [2,4,6,8,10];
console.log(arr.some((x) => x > 8));