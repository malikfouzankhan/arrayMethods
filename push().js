// push() method
// inserts an element after the last element of the array
// NOTE : push() is an in-place method which means it will change the original array

let arr = [1,2,3,4];
// console.log(arr.push(5));
arr.push(5);
console.log(arr);