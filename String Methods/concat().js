// concat() method
// returns a string with both the string's content added
// string concat() method is equivalent to '+' operator

let str1 = "Malik";
let str2 = "Fouzan";

console.log(str1.concat(str2));
// it is equivalent to:
console.log(str1 + str2);