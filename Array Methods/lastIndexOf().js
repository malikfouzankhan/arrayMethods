// lastIndexOf() method
// returns the index of the given value but starts searching from the end of the array
// returns -1 if the value does not exist

let arr = [100,200,300,100,500];
console.log(arr.lastIndexOf(101));