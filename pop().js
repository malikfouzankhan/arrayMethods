// pop() method
// removes and returns the last element of the array
// NOTE : pop() is an in-place method which means it will change the original array

let arr = [1,2,3,4,5,6,7,8,9,0];
// console.log(arr.pop());
arr.pop();
console.log(arr);